// Define an array of names
const names: string[] = ["Mustufa", "Hammad", "Irfan", "Burhan", "Hamza"];

// Iterate over the array and print each name
names.forEach(name => {
    console.log(name);
});
